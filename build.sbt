import AssemblyKeys._ // put this at the top of the file

assemblySettings

name := "parallel-plovr-build"

version := "1.0"

scalaVersion := "2.9.1"

libraryDependencies += "org.scalatest" %% "scalatest" % "1.8" % "test"
